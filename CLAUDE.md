# 『関係の温度と距離を整える技術』診断サイト ── Claude Code 作業指示書

`index.html` が動作するプロトタイプ。これを起点に開発する。

---

## 更新（2026-07 ／ Bルート確定）── 以下が最新。旧記述と食い違う場合はこちらを優先

- **本体は `kankei/index.html`（`/kankei`＝恋愛と関係の診断）。** ルート `/` は `/kankei` へリダイレクト（`vercel.json` ＋ `index.html`）。組織版 `/soshiki` は未作成（別途依頼）。旧 `index.html`（温度/組織＋距離/恋愛の2デッキ・ダーク版）は git 履歴に保存。
- **1つの診断・全8問が、温度と距離を同時に測る。単一選択。** Q1〜Q7 は投稿と一字一句同じ。Q8 はサイト限定（投稿に出さない）。配点・スコアリング・4タイプは仕様書（このリポジトリ外の指示）に準拠。型名は **溶ける／秘める／尽くす／守る**（確定。HL=秘める人＝温度高・距離低＝感情はあるが渡す相手がいない・締めは「届いていない」。LH=尽くす人＝温度低・距離高＝相手はいるが渡すのが自分の感情でない・締めは「自分がいない」。この2型は"熱量の差"では書き分けない）。
- **温度軸**＝凍結/隠す/選ぶ/溢れる（1→4）、**距離軸**＝閉じる/守る/測る/明け渡す（1→4）。各合計8〜32、21以上=高。
- **出口はメンバーシップ1本。診断サイトから note へはリンクしない。** プラン名を改称：`温度の整え方 → チームCoco ―― 組織と仕事` ／ `距離の整え方 → チームCoco ―― 恋愛と関係`（商品名『関係の温度と距離を整える技術』は不変）。
- **デザインは cream 系**：クリーム `#fbf8f1`／ペーパー `#f1ece1`／アンバー `#a8763c`、見出し Shippori Mincho B1・本文 Zen Kaku Gothic New。モバイルファースト、1問ずつ表示・進捗表示。※下の「3. デザイン方針」のダーク配色は旧 `index.html` 用。
- **Threads診断の締めはサイト誘導に統一。** 設問1通目は URL を入れず、単一選択（「A〜Dで教えて。」）。返信欄は**位置を示すだけ**——各選択肢は「傾向」まで（代償・処方は書かない）、旧・結論ブロックは削除、「どこに立っているか」に振ってから、**行動は1つ＝その日の診断リンク `/kankei/MM-DD` のみ**（フォロー等の逃げ道を並べない。三段の鉄則「1つの場所に、1つの行動」。返信欄まで読む人は既にフォロワーに近く、フォロー導線は"クリックしない言い訳"になる）。7/20 は「残る1問（弱っている・サイト限定）」フックを入れる。
- **結果画面（8問後）の出口はメンバーシップ1本。** 2択・逃げ道を置かない（答え切った人は最も熱量が高い。ここで軽い選択肢を並べると割れる）。無料で渡すのは説明（②③）、有料に留保するのは処方＝整える順番（④＝チームCoco）。
- **投稿アーカイブは track ごとに分けて蓄積：** 恋愛と関係 = `posts/kankei/`（現在7本）、組織と仕事 = `posts/soshiki/`（準備中）。共有 CSS/JS は `posts/assets/`、ハブは `posts/index.html`。
- **閾値は「実分布の中央値」（ドメイン別・`SHINDAN.tempCut`/`distCut`）。レンジ中点20ではない。** 軸は1〜4だが対称でなく、中心＝3（適温・適距離）＝3×8=24。恋愛=温23/距25、組織=温21/距22。現在地マップも同じ中心に補正（型と光る象限が一致）。**配点や設問を変えたら、4^8=65,536通りを全列挙して4型が各15〜35%に収まるか必ず再チェック**（怠ると1型に偏る。恋愛版で閾値21のとき溶ける人79.7%の事故が起きた）。
- **中心（保つ人）＝5つ目の状態。2×2は中心を表現できない。** 「ほぼ整っているが1問だけズレた人」（例：全問3・1問だけ距離を守り側＝温24/距23）を4型に寄せると「まだ誰にも届いていない（秘める）」等と嘘をつく。中心＝**マンハッタン距離 |Δ|≤1**（`SHINDAN.center = {t:24,d:24,r:1}`＝24/24とそこから1軸だけ1ズレた人）を別状態に分離＝`SHINDAN.CENTER`（型名 **保つ人**＝動詞で他4型と揃える／※`測る`は距離軸ラベルと衝突するので不採用。Coco最終確定待ち）。**この型だけメンバーシップCTAを出さず、代わりに「ここまで整っている人は稀。ではなぜこの問いが気になったのか」という別の問いを置く**（整っている人は整え方を買わない＝元から売れない層。CTAを外す損失＝見かけの露出だけ／得るもの＝診断が嘘をつかない事実。1件のCVより診断の信頼性が上）。**制約の取り違え注意：目的は「診断が嘘をつかないこと」であって「中心を数%に抑えること」ではない。** 出現率が恋愛10.9%／組織3.4%でも、そこは最もメンバーシップに遠い層なので問題ない（24/24ちょうど＝2.2%/1.1%まで潰すのは締めすぎ＝却下済み）。**中心を分離した最終5分布（4^8列挙）：恋愛＝保10.9/溶30.7/秘15.9/尽17.3/守25.2、組織＝保3.4/背27.4/こ19.4/引26.0/離23.8（4型は全て15〜35%）。** ※`|Δ|≤2` に広げると恋愛25.5%・秘める10.2%で型が15%を割るので不可＝`r:1` が上限。
- **共有エンジンは汎用化済み（2ドメイン）：** `/assets/shindan.{css,js}` が `window.SHINDAN`（ドメインデータ）＋ `window.ENTRY`（入口）を読む。**軸は温度×距離のまま（商品名の核なので割らない）／読み方・設問・型・結果文だけドメイン別。** 恋愛＝`/kankei`（`/kankei/data.js`）、組織＝`/soshiki`（`/soshiki/data.js`）。ルート `/` は **組織／恋愛の選択画面**（`/→/kankei` リダイレクトは解除）。各ドメインに 入口ページ `MM-DD.html`（`window.ENTRY={start,home}`）＋ 読者ハブ `index.html`。
- **組織版（`/soshiki`）：** 温度＝言葉の温度をどう扱うか／距離＝誰の課題をどこまで自分に入れるか。型は **背負う／こらえる／尽くす→引き取る／離れる**（恋愛の 溶ける／秘める／尽くす／守る に1:1対応、名称は仮）。**こらえる×引き取るは「相手がいるか・渡すのが自分の判断か」で割る（熱量で割らない）：** こらえる＝相手に渡していない（届いていない）／引き取る＝相手には渡すが、渡すのは役割で判断ではない（自分がいない）。締めの28本は仕様で確定・語尾は全て別。X診断の返信欄は **コメント②（処方の手順）を削除**し、各日の診断 `/soshiki/MM-DD` へ誘導（note へは出さない。note は本文＝別funnelの単発販売）。
- **曜日別URL（`/kankei/MM-DD`）＝本体診断への入口：** その日の問いを1問目にして本体（8問→温度×距離の4タイプ）を回す（進捗1/8から／既に答えを決めて来ているので1問目のハードルがゼロ）。共有エンジン `kankei/assets/kankei.{css,js}`、本体 `kankei/index.html`（既定順）＋各曜日 `kankei/07-14.html`〜`07-20.html`（`window.KANKEI={start,home}` を置くだけ）。設問後に完了画面＋結果冒頭に「診断結果」バナー。**Q8（弱っている）は常に最後・サイト限定。** 旧・二極（どっち側）ページは撤去済み。結果は温度×距離の4タイプなので、告知は「どっち側か」ではなく**「どこに立っているか」**で揃える。
- **結果ブロック③は入口の日×型で28通り（`DAY_ANSWER` in `kankei.js`）。** 読者は「型」ではなく"その日の問い"を抱えて来ているので、③はその日の問いをそのまま繰り返し→型の核がその答えだと示し→「〜じゃない。〜だっただけだ」で閉じる。既定 `/kankei`（入口なし）は型別の汎用③にフォールバック。
  - **4型の着地は固定：** 溶ける＝全部を一度に渡して自分の分が残らない／秘める＝まだ誰にも届いていない／尽くす＝その関係にあなた自身がいない／守る＝閉じたまま動かしていない。この4方向に割れていること。
  - **③で「どうするか（処方）」を渡さない。** 処方は④（メンバーシップ）の領域。③に処方を書くと④を買う理由が消える（例：守るの締めを「温度が足りない」＝処方にしない）。
  - **型名で説明しない。** 「溶ける人だから溶けている」は同語反復。型名を使わずに機構を説明できているか、締めを毎回検算する。
  - **来週以降の週次バッチは、着地の言い回しを型の中で変える設計にする**（同じ語尾を7問連発するとリピーターにテンプレートが露出する）。※現状の既知事項：守る（LL）の7本は締めが一字一句同じ「閉じたまま動かしていないだけだ」×7、秘める＝「まだ誰にも届いていない」×7、尽くす＝「あなた自身がいない」×7。軸は保ったまま、来週バッチで言い回しを型内で振る（今週は1型しか見えないため出荷可）。
- **結果画面は4ブロック（①型と2軸の位置 → ②選んだ選択肢を動的引用して1本に繋ぐ → ③最初の問いに答える → ④メンバーシップCTA）。②③④は4タイプ分を書き分け（汎用テンプレ禁止）。** 無料で渡すのは「説明」、有料に留保するのは「処方」。
- 語彙ルール（下記「4. 文章ルール」）は継続。ただし結果画面のコピーは仕様書の例文に合わせ「あなた」を限定的に使用（§4の不使用ルールと相反する箇所は仕様書の例文を優先）。

---

## 1. このサイトの目的

SNS（TOFU）と note 有料部分（BOFU）の間を埋める「自分ごと化」の一段。
診断で読者がタイプを確定した直後に、メンバーシップへ渡す。

CTA の優先順位（結果ページの並び順は変更しない）：

1. メンバーシップ登録（最優先）
2. SNS シェア
3. LINE / DM
4. note 有料記事

---

## 2. 構成

- 入口で 2 診断に分岐
  - **温度の診断**（組織と仕事）→ メンバーシップ『温度の整え方』へ
  - **距離の診断**（恋愛と関係）→ メンバーシップ『距離の整え方』へ
- 各 8 問（2軸 × 4問）／二者択一／1画面1問／戻る可
- 判定：各軸の合計符号で 4 タイプ

| 診断 | X軸 | Y軸 |
|---|---|---|
| 温度 | HEAT ⇔ COOL | SELF ⇔ OTHER |
| 距離 | NEAR ⇔ FAR | SPEAK ⇔ HOLD |

タイプ：過熱型／供給型／断熱型／待機型、密着型／追随型／宣言型／撤退型

---

## 3. デザイン方針

- 署名要素は **現在地マップ**（2軸フィールド上の光点）。設問中も薄く表示し、回答のたびに点が移動する。ここ以外の装飾は増やさない
- 配色：ink `#0F1114` ／ 温度 = 琥珀 `#C08A3E` ／ 距離 = 氷 `#7E97A8`
- 書体：見出し Shippori Mincho B1、本文 Zen Kaku Gothic New、ラテン Cormorant Garamond
- 余白を削らない。アニメーションを足さない。`prefers-reduced-motion` を守る

---

## 4. 文章ルール（厳守）

- 本文に「あなた」「みんな」を使わない
- 「〜んです」「〜でいい」「〜でもいい」を使わない
- 断定の「だ／だった」は可。1行1呼吸。詩的な断片化はしない
- 結果ページは **構造の指摘**。温度版＝冷徹な軍師の声、距離版＝慈愛に満ちた哲学者の声。声を混ぜない
- 「視点の置き換え」は行動指示（How）にしない。売っているのは在り方（Being）であり、結果ページで解決しきらない
- メンヘラ感・強がり感・無理してる感を出さない

---

## 5. 次にやること（優先順）

1. `LINKS` の URL を実際のものに差し替える（`index.html` 冒頭）
2. 結果を URL に持たせる：`?d=work&t=aa` で結果ページに直接入れるようにする（シェア経由の流入を結果ページで受ける）
3. タイプ別 OGP 画像 8 枚（`/og/work-aa.png` ...）を生成し、`<meta property="og:image">` を結果状態で書き換える
4. 結果カードの画像書き出し（Canvas → PNG）。Threads / Instagram のシェア用
5. GA4 または Plausible を導入。計測イベント：`start`（診断種別）、`complete`（タイプ）、`cta_click`（種別）
6. Vercel または Netlify に静的デプロイ。独自ドメインをあてる

## やらないこと

- バックエンド、DB、ログイン
- 回答の保存（localStorage も使わない）
- タイプ数を 4 以上に増やす（設問 8 問で支えられる解像度を超える）
